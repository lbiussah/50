nm = 15
st = '15'
lst = ['15']

def to_float(num):
    if num == int(num):
        num_float = float(num)
        return num_float
    else:
        return 'Невозможно преобразовать'
    
print(to_float(st))
